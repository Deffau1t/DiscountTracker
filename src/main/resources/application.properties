# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=qdaqda9@gmail.com
spring.mail.password=vlzz pfml ibva zxkn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Database Configuration
spring.datasource.url=jdbc:h2:file:./data/discount_tracker
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# H2 Console (for development)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Data Initialization
spring.sql.init.mode=never

# Recommendation System Configuration
recommendation.min.weight=0.5
recommendation.min.similarity=0.3
recommendation.default.limit=10
recommendation.matrix.factors=10
recommendation.cluster.k=5

# Algorithm Weights
recommendation.algorithm-weights.content-based=0.25
recommendation.algorithm-weights.collaborative=0.2
recommendation.algorithm-weights.matrix-factorization=0.15
recommendation.algorithm-weights.clustering=0.1
recommendation.algorithm-weights.temporal=0.1
recommendation.algorithm-weights.trend-based=0.1
recommendation.algorithm-weights.personalized=0.1

# Trend Analysis Settings
recommendation.trend-settings.min-price-history-size=5
recommendation.trend-settings.positive-trend-threshold=0.05
recommendation.trend-settings.popularity-growth-threshold=0.1
recommendation.trend-settings.seasonality-threshold=0.5

# Personalization Settings
recommendation.personalization-settings.time-weight=0.1
recommendation.personalization-settings.source-weight=0.3
recommendation.personalization-settings.price-weight=0.2
recommendation.personalization-settings.category-weight=0.4
recommendation.personalization-settings.min-interactions-for-analysis=5 